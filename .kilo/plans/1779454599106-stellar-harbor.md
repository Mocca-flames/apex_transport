# Plan: Eliminate Unnecessary Whitespace on Mobile

## Issue Identified
Line 125 in index.html: `<div class="container--narrow container">` — double container classes is redundant CSS.

## Analysis of Mobile Whitespace Issues

### 1. Double Container Class (index.html:125)
```html
<div class="container--narrow container">
```
Both `.container` and `.container--narrow` set `width: 100%` and `margin-inline: auto`. The combination is redundant.

**Fix:** Remove `container` and keep only `container--narrow`.

### 2. Trust Bar Mobile Padding (layout.css:286-290)
On ≤640px:
- `.trust-bar__item` has `padding: var(--space-4) var(--space-2)` — may still be excessive
- Consider reducing to `var(--space-2) var(--space-1)`

### 3. Form Grid Gap (index.html:517)
```html
style="--form-cols:1fr 1fr; display:grid; grid-template-columns:var(--form-cols); gap: var(--space-4);"
```
On mobile, `gap: var(--space-4)` (16px) could be reduced.

### 4. Contact CTA Stack Gap (layout.css:314)
```css
@media (max-width: 640px) {
  .contact-cta-stack {
    gap: var(--space-6);  /* 24px */
  }
}
```

### 5. Section Padding on Mobile
Check `--section-pad-sm` and `--section-pad-md` usage for excessive spacing.

## Implementation Steps

### index.html
1. Line 125: Change `class="services-sticky">\n\n\n        <!-- Left: text column -->\n        <div class="services-text">` → remove redundant container classes
   - Current: `container--narrow container`
   - New: `container--narrow`

### layout.css
1. Mobile trust-bar padding (lines 286-290):
   - Current: `padding: var(--space-4) var(--space-2);`
   - New: `padding: var(--space-2) var(--space-1);`

## Files to Modify
1. `index.html` — remove redundant `container` class on line 125
2. `layout.css` — reduce trust-bar mobile padding from space-4/space-2 to space-2/space-1

## Files to Modify
1. `index.html` — remove redundant container class
2. `layout.css` — adjust trust-bar mobile padding if needed

## Verification
Test on mobile viewport (≤640px) for:
- Trust bar content not squished
- Form fields readable
- Sections have appropriate breathing room without excessive scroll