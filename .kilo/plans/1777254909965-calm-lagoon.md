# Plan: Update Locations & Fix Mobile Copywriting

## Current State
- **Coverage section** shows only 4 countries: South Africa, Zimbabwe, Zambia, DRC
- **Mobile view** displays long text blocks that are difficult to read on small screens
- User wants to add: Botswana, Zambia, Malawi, Zimbabwe, Tanzania, South Africa, Mozambique, DRC (8 total)

## Changes Required

### 1. Update Coverage Section HTML (lines 1702-1742)
- Replace current 4-country grid with all 8 countries
- Structure: 2 columns × 4 rows grid
- Countries with flags and notes:
  - South Africa: "HQ & Primary Hub"
  - Zimbabwe: "Active corridors"
  - Zambia: "Active corridors"
  - DRC: "Specialist routing"
  - Botswana: "Active corridors"
  - Mozambique: "Active corridors"
  - Malawi: "Active corridors"
  - Tanzania: "Active corridors"
- Structure: 2 columns × 4 rows grid on desktop, responsive at breakpoints
- Maintain responsive behavior: grid auto-adjusts at breakpoints

### 2. Update Footer Coverage Links (lines 1950-1958 & 1971)
- Add missing countries to footer navigation links
- Update bottom footer line to list all 8 countries

### 3. Update Ticker (lines 1512-1527)
- Replace current 4-country ticker with all 8 countries (South Africa, Zimbabwe, Zambia, DRC, Botswana, Mozambique, Malawi, Tanzania)
- List them twice to fill the scrolling animation: [SA → Zimbabwe → Zambia → DRC → Botswana → Mozambique → Malawi → Tanzania → repeat ×2]
- Keep the ticker-dot separator between each country

### 4. Update Form Country Dropdowns (lines 1897-1916)
- Add missing country options to both Origin and Destination selects
- Keep "Other" option for non-covered countries

### 5. Mobile Copywriting Fixes (CSS Media Queries)
Apply to `@media (max-width: 640px)`:

- **Hero subtitle** (`.hero-sub`): Clamp to 3 lines max with ellipsis
- **Services intro** (`.services-intro`): Clamp to 4 lines max
- **Coverage section body** (`.section-body` in coverage): Clamp to 4 lines max
- **Service card descriptions** (`.service-desc`): Clamp to 4 lines max, reduce font size to 14px
- **Step descriptions** (`.step-desc`): Clamp to 4 lines max, reduce font size to 14px
- **Value card descriptions** (`.value-desc`): Clamp to 4 lines max, reduce to 13px
- **Tech banner body** (`.tech-body`): Clamp to 5 lines max
- **Contact section body** (`.section-body` in contact): Clamp to 3 lines max

Implementation: Use `display: -webkit-box; -webkit-line-clamp: N; -webkit-box-orient: vertical; overflow: hidden;` for each element type at mobile breakpoint.

### 6. Optional: Truncate Country Notes on Mobile
- `.country-note`: Clamp to 1 line with ellipsis on mobile to prevent overflow

### 7. Testing & Validation
- Visual check at 375px width (iPhone SE)
- Ensure no horizontal overflow
- Verify all 8 countries appear in Coverage grid
- Confirm trackpad/touch scrolling works smoothly
- Check that truncated text still conveys core message

## Files to Modify
- `index.html` (single file, all changes contained)

## Estimated Complexity
- Low: HTML structure changes + targeted CSS additions
- No JavaScript changes required
- No external dependencies
